[project]
name = "tkinter-icons-lucide"
version = "1.1.0"
description = "Lucide Icons provider for tkinter-icons"
readme = "README.md"
requires-python = ">=3.10"
authors = [{ name = "Israel Dryer" }]
dependencies = ["Pillow>=9.1", "tkinter-icons>=5.0.0"]

license = "MIT"

[project.urls]
Homepage = "https://github.com/israel-dryer/tkinter-icons"

# Auto-discovery hook for the base registry
[project.entry-points."tkinter_icons.providers"]
lucide = "tkinter_icons_lucide.provider:LucideFontProvider"

[project.scripts]
tkicons-lucide-build = "tkinter_icons_lucide.tools.generate_assets:main"
tkicons-lucide-quick = "tkinter_icons_lucide.tools.generate_assets:default_main"

[tool.setuptools.package-data]
"tkinter_icons_lucide" = ["fonts/*.ttf", "glyphmap*.json", "metrics*.json", "LICENSES/*"]

[tool.setuptools]
package-dir = { "" = "src" }
include-package-data = true

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"


