[project]
name = "ttkbootstrap-icons-typicons"
version = "0.1.0"
description = "Typicons (font-based) provider for ttkbootstrap-icons"
readme = "README.md"
requires-python = ">=3.10"
authors = [{ name = "Israel Dryer" }]
dependencies = ["Pillow>=9.1", "ttkbootstrap-icons>=2.0.0"]

license = "MIT"

[project.urls]
Homepage = "https://github.com/israel-dryer/ttkbootstrap-icons"

[project.entry-points."ttkbootstrap_icons.providers"]
typicons = "ttkbootstrap_icons_typicons.provider:TypiconsProvider"

[project.scripts]
ttkicons-typicons-build = "ttkbootstrap_icons_typicons.tools.generate_assets:main"
ttkicons-typicons-quick = "ttkbootstrap_icons_typicons.tools.generate_assets:default_main"

[tool.setuptools.package-data]
"ttkbootstrap_icons_typicons" = ["fonts/*.*", "glyphmap.json", "LICENSES/*"]

[tool.setuptools]
package-dir = { "" = "src" }
include-package-data = true

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

