[project]
name = "ttkbootstrap-icons-fluent"
version = "0.1.0"
description = "Fluent Icons (font-based) provider for ttkbootstrap-icons"
readme = "README.md"
requires-python = ">=3.10"
authors = [{ name = "Israel Dryer" }]
dependencies = ["Pillow>=9.1", "ttkbootstrap-icons>=1.0.0"]

license = "MIT"

[project.urls]
Homepage = "https://github.com/israel-dryer/ttkbootstrap-icons"

# Auto-discovery hook for your base registry
[project.entry-points."ttkbootstrap_icons.providers"]
fluent = "ttkbootstrap_icons_fluent.provider:FluentFontProvider"

[project.scripts]
ttkicons-fluent-build = "ttkbootstrap_icons_fluent.tools.generate_assets:main"
ttkicons-fluent-quick = "ttkbootstrap_icons_fluent.tools.generate_assets:default_main"

[tool.setuptools.package-data]
"ttkbootstrap_icons_fluent" = ["fonts/*.ttf", "glyphmap.json"]

[tool.setuptools]
package-dir = { "" = "src" }
include-package-data = true

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

