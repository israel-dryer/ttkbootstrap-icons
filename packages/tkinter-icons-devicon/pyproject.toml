[project]
name = "tkinter-icons-devicon"
version = "1.1.0"
description = "Devicon (font-based) provider for tkinter-icons"
readme = "README.md"
requires-python = ">=3.10"
authors = [{ name = "Israel Dryer" }]
dependencies = ["Pillow>=9.1", "tkinter-icons>=5.0.0"]

license = "MIT"

[project.urls]
Homepage = "https://github.com/israel-dryer/tkinter-icons"

# Auto-discovery hook for the base registry
[project.entry-points."tkinter_icons.providers"]
devicon = "tkinter_icons_devicon.provider:DeviconFontProvider"

[project.scripts]
tkicons-devicon-build = "tkinter_icons_devicon.tools.generate_assets:main"
tkicons-devicon-quick = "tkinter_icons_devicon.tools.generate_assets:default_main"

[tool.setuptools.package-data]
"tkinter_icons_devicon" = ["fonts/*.ttf", "glyphmap*.json", "metrics*.json", "LICENSES/*"]

[tool.setuptools]
package-dir = { "" = "src" }
include-package-data = true

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

