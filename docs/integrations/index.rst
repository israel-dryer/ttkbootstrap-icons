Integrations
============

An icon's :attr:`~tkinter_icons.Icon.image` is an ordinary Tk-compatible image, so it works with anything that takes one. These pages cover the idioms of each framework rather than repeating the API.

.. grid:: 1 2 2 2
   :gutter: 3

   .. grid-item-card:: tkinter and ttk
      :link: tkinter-ttk
      :link-type: doc

      Buttons, menus, trees, notebooks, canvases, and the window icon — plus the reference-keeping rule that catches everyone once.

   .. grid-item-card:: ttkbootstrap
      :link: ttkbootstrap
      :link-type: doc

      Using another icon set inside ttkbootstrap, and letting the theme choose the color.

.. toctree::
   :hidden:

   tkinter-ttk
   ttkbootstrap
